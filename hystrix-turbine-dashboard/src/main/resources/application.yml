application:
  clusters: storage-service-db1, storage-service-db2, read-service, write-service
turbine:
  aggregator:
    clusterConfig: ${application.clusters}
  appConfig: ${application.clusters}

---

spring:
  profiles: dev

---

spring:
  profiles: prod